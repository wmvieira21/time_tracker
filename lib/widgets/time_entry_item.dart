import 'package:flutter/material.dart';

class TimeEntryItem extends StatelessWidget {
  const TimeEntryItem({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
